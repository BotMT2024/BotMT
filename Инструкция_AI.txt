Для запуска сервера необходимо запустить файл server.py командой:

uvicorn main:app --host 0.0.0.0 --port 80

После необходимо зайти в браузере по адресу:
http://localhost:8000/docs

Далее откроется интерфейс, в котором (во вкладке upload) можно протестировать данный микросервис